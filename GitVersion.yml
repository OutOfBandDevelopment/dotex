mode: ContinuousDeployment

branches:
  main:
    increment: Patch
    track-merge-target: false

  feature:
    regex: ^(?!main$).*
    increment: Inherit
    source-branches: ['main']
    track-merge-target: false

ignore:
  sha: []
